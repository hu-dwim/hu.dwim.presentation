
WUI

Full lisp web server, based on iolib with some borrowed ideas from UCW,
but as a new codebase.

(asdf:operate 'asdf:test-op :wui)

(start-test-server-with-wudemo-application)

http://localhost.localdomain:8080/


DOJO

# next to the wui/ directory:
svn co http://svn.dojotoolkit.org/src/view/anon/all/trunk dojo
wui/etc/build-dojo.sh
