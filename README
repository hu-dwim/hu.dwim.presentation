1. WUI
------

An all-lisp web server, based on iolib.

(asdf:operate 'asdf:test-op :wui)

(start-test-server-with-wudemo-application)

http://localhost.localdomain:8080/

(setf hu.dwim.wui::*debug-on-error* t)

2. DOJO
-------

You need to build dojo for WUI to work:

$ svn co -r 17259 http://svn.dojotoolkit.org/src/trunk/ dojo/
$ ~/workspace/wui/etc/build-dojo.sh --dojo ~/workspace/dojo.head --dojo-release-dir ~/workspace/wui/wwwroot/ --profile ~/workspace/wui/etc/wui.profile.js --locales "en-us,hu"
